---
const { title, containerClass } = Astro.props;
---

<h2 class={`${containerClass} `}>{title}</h2>

<!-- <script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("DOMContentLoaded", function () {
    // Animación de ScrollTrigger
    const titleAnimation = gsap.timeline({
      scrollTrigger: {
        trigger: ".animated-title",
        start: "top bottom",
        end: "center bottom",
        toggleActions: "play none none reverse",
      },
    });

    titleAnimation.to(
      ".animated-word",
      {
        opacity: 1,
        transform: "translate3d(0, 0, 0) rotateY(0deg) rotateX(0deg)",
        ease: "power2.inOut",
        stagger: 0.02,
      },
      0
    );
  });
</script> -->

<!-- <script>
  import gsap from "gsap";
  import SplitType from "split-type";
  import { ScrollTrigger } from "gsap/all";

  gsap.registerPlugin(ScrollTrigger);

  const text = gsap.utils.toArray("h2");
  text.forEach((text) => {
    const img = text.nextElementSibling;
    const splitType = new SplitType(text);

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: text,
        start: "top 80%",
        end: "bottom top",
        toggleActions: "play play none reverse",
      },
    });

    tl.from(splitType.chars, {
      opacity: 0,
      y: 50,
      stagger: 0.005,
      duration: 1,
      ease: "power3.out",
    })

      .from(
        img,
        {
          y: 100,
          opacity: 0,
          duration: 2,
        },
        "<"
      );
  });
</script> -->
